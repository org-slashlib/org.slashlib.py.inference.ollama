```powershell
# upgrade pip to version matching version of python
PS> python -m pip install --upgrade pip setuptools wheel
PS> pip install --upgrade build
PS> pip install --upgrade twine

# die Nutzung des configloaders ermöglichen
PS> pip install org.slashlib.py.configloader

# den Zugriff auf ollama vorbereiten
PS> pip install ollama

# Unittesting und Codecoverage vorbereiten
PS> pip install pytest pytest-asyncio pytest-cov
```